import UserInfo from "./UserInfo";
// import UserContext from "../context/UserContext";

const User = () => {
  return (
    // <UserContext.Provider value={{ user: "Dude " }}>
    <UserInfo />
    // </UserContext.Provider>
  );
};

export default User;
