import UserInfo from "./UserInfo";
// function User({ user, setUser }) {
//   return <UserInfo user={user} setUser={setUser} />;
// }

const User = () => {
  return <UserInfo />;
};

export default User;
